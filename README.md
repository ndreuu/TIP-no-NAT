# TIP-no-NAT
TIP benchmarks with replaced NAT constructors constructors


TIP.Original.Linear contains original TIP
TIP.only-nat-constructors.csv contains modified benchmarks that contained only NAT constructors
TIP.not-only-nat-constructors contains modified benchmarks that contained not only NAT constructors

